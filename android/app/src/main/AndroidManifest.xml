<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- SECURITY: allowBackup was true. With it on, `adb backup` (and
         cloud / device-to-device transfer) can lift the app's private data
         off the device without root - here that means the WebView
         localStorage holding the session token, the customer account
         records and the admin credential hash. Nothing in this app needs
         backing up; it all rebuilds from the server or a fresh sign-in.
         dataExtractionRules is the Android 12+ equivalent. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Rider links (…/ride/#CODE) sent on WhatsApp open in this app
                 when it is installed; otherwise they open in the browser as
                 before. autoVerify checks /.well-known/assetlinks.json on the
                 site, which names this package and its signing key. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="modern-dairy-pune.web.app" android:pathPrefix="/ride" />
                <data android:scheme="https" android:host="modern-dairy-pune.firebaseapp.com" android:pathPrefix="/ride" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Location: used only when the customer taps "Use my current location"
         at signup to pre-fill their delivery address. Coarse + fine so it works
         whether they grant approximate or precise location. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Rider live tracking. The delivery rider cannot keep the app open and
         the screen on while riding, so location has to keep flowing with the
         phone pocketed. Android only permits that from a FOREGROUND SERVICE,
         which is required by law to show a persistent notification — the
         rider always sees that tracking is running and can stop it.
         ACCESS_BACKGROUND_LOCATION is requested separately by the OS and the
         user must pick "Allow all the time"; anything less and Android cuts
         updates off a few seconds after the screen locks.
         Only used while a delivery is in progress — see riderStartTracking(). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
