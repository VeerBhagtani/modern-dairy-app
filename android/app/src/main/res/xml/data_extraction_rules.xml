<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ counterpart to allowBackup="false": exclude everything from
     both cloud backup and device-to-device transfer. The app holds a session
     token, customer account records and the admin credential hash in WebView
     local storage; none of it should leave the device in a backup image. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
