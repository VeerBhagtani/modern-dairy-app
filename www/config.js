/* ═══════════════════════════════════════════════════════════
   BUILD / ENVIRONMENT CONFIG — PLACEHOLDER COPY

   Do not edit this file to point at a server. CI overwrites it at
   build time from the build environment (scripts/inject-config.js),
   and scripts/preflight.js fails the build if the committed copy is
   anything other than the demo placeholder below. To build against a
   real backend, set API_BASE (and RAZORPAY_KEY) in the build
   environment instead — see DEPLOYMENT.md.

   Everything here is also overridden by the backend /config endpoint
   at runtime once API_BASE is set, so the Admin Panel can change it
   without an APK rebuild.
   ═══════════════════════════════════════════════════════════ */
window.APP_CONFIG = {
  API_BASE: '',            // set via the API_BASE build variable, e.g. https://<cloud-run-url>/api
  DEMO: true,              // true = works fully offline; flipped off automatically when API_BASE is set
  RAZORPAY_KEY: '',        // publishable key id only (rzp_live_… / rzp_test_…) — never the key secret
};
